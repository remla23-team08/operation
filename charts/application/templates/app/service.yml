{{- with .Values.app }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .name }}-svc
spec:
  selector:
    app: {{ .name }}
  ports:
    - port: {{ .port }}
      targetPort: {{ .port }}
{{- end }}
