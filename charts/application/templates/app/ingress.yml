{{- with .Values.app }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .name }}-ingress
spec:
  defaultBackend:
    service:
      name: {{ .name }}-svc
      port:
        number: {{ .port }}
{{- end }}